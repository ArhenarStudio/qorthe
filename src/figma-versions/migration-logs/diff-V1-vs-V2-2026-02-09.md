# Diff Report: DSD V1 → DSD V2

**Fecha:** 2026-02-09  
**Generado por:** Cursor (automático)

---

## Resumen

| Tipo | Cantidad |
|------|----------|
| Archivos nuevos | 1 |
| Archivos eliminados | 0 |
| Archivos modificados | 2 |
| Sin cambios | 20+ (resto de componentes en `src/app/components/`) |

---

## 🆕 Archivos nuevos

| Archivo | Descripción |
|---------|-------------|
| **AnimatedSection.tsx** | Componente de animación con **motion/react** (Framer Motion). Exporta `AnimatedSection`, `StaggerContainer` y `staggerItem` para animaciones al entrar en viewport (dirección up/down/left/right, stagger en hijos). Dependencia: `motion/react`. |

---

## ✏️ Archivos modificados

### 1. Footer.tsx

| Aspecto | Detalle |
|--------|---------|
| **Imports** | V2 añade: `Facebook`, `Twitter`, `Instagram`, `Youtube`, `Linkedin` de `lucide-react`. |
| **Props** | Nueva prop opcional: `onNavigateCatalog?: () => void`. Catálogo pasa a ser un `<button onClick={onNavigateCatalog}>` en vez de `<a href="#">`. |
| **Contenido / copy** | Descripción de empresa ya no usa `t.footer.description`: usa texto fijo por idioma: *"Una herencia sin nombre que hoy encuentra forma. Artesanía consciente..."* (ES) / *"A nameless heritage that finds form today..."* (EN). |
| **Estructura** | Layout del footer reordenado: bloque principal con más padding (`py-16 md:py-20 lg:py-24`). Logo + descripción centrados arriba (título más grande `text-4xl md:text-5xl lg:text-6xl`). Debajo, **3 columnas**: Navegación, Contacto, **Redes Sociales**. |
| **Redes sociales** | **Nueva sección** "Síguenos" / "Follow Us" con iconos: Facebook, X (Twitter), Instagram, YouTube, TikTok (SVG inline), Pinterest (SVG inline), LinkedIn. Enlaces a facebook.com, x.com, instagram.com, youtube.com, tiktok.com, pinterest.com, linkedin.com (con path `davidsonsdesign`). |
| **Barra inferior** | Copyright, enlaces legales (Privacidad, Términos) y toggle de tema pasan a estar en una fila central (orden responsive con `order-1/2/3`). Separadores entre enlaces legales (punto). "Powered by **RockStage**" con "RockStage" en negrita y color `#8b6f47`. |
| **Nota** | En el proyecto Next.js el Footer actual tiene además `onNavigateCookies` y enlace a Cookies; en V2 Figma no aparece. **Mantener** `onNavigateCookies` y enlace a cookies al migrar. |

---

### 2. LandingPage.tsx

| Aspecto | Detalle |
|--------|---------|
| **Imports** | V2 añade: `useRef` de React; `motion`, `useScroll`, `useTransform`, `useInView` de `motion/react`; `AnimatedSection`, `StaggerContainer`, `staggerItem` de `./AnimatedSection`; iconos de redes (Facebook, Twitter, Instagram, Youtube, Linkedin) de lucide. |
| **Copy** | Misma actualización de descripción que en Footer: texto "Una herencia sin nombre..." / "A nameless heritage..." en `hero.description` (ES/EN). |
| **Animaciones** | Hero: contenedor y título con `motion.div` / `motion.h2` / `motion.span` (opacity, x, y, delays). Botón CTA con `motion.button` (whileHover scale 1.05, whileTap 0.95). Imagen hero con `motion.div` (opacity, scale) y hover scale 1.02. |
| **Secciones** | Título de "Nuestras Colecciones" envuelto en `<AnimatedSection>`. Grid de colecciones usa `<StaggerContainer>` y cada tarjeta es `<motion.div variants={staggerItem}>` con hover scale 1.02 en la imagen. Sección "Proceso" usa `<AnimatedSection direction="left">` y `motion.div` con hover en la imagen. |
| **Dependencia** | **motion/react** (Framer Motion) es necesaria para compilar y ejecutar LandingPage V2. |

---

## ✅ Sin cambios (componentes comparados)

Los siguientes archivos existen en ambos y **no tienen diferencias** (mismo contenido):

- Header.tsx  
- CartDrawer.tsx  
- CartPage.tsx  
- CheckoutPage.tsx  
- CookiePolicy.tsx  
- LoginPage.tsx  
- OrderConfirmationPage.tsx  
- OrderDetailPage.tsx  
- OrdersPage.tsx  
- PaymentPage.tsx  
- PrivacyPolicy.tsx  
- ProductCatalog.tsx  
- ProductDetail.tsx  
- RegisterPage.tsx  
- TermsAndConditions.tsx  
- AccountDashboard.tsx  
- AddressesPage.tsx  
- AddressModal.tsx  
- WishlistPage.tsx  
- figma/ImageWithFallback.tsx  

*(Los archivos dentro de `ui/` no se compararon uno a uno; se asumieron sin cambios salvo que se indique lo contrario.)*

---

## 📋 Resumen para migración

1. **Footer (Next.js:** `src/modules/footer/components/Footer.tsx`)  
   - Aplicar: nuevo layout (logo centrado + 3 columnas), nueva descripción de empresa, columna de redes sociales (Facebook, X, Instagram, YouTube, TikTok, Pinterest, LinkedIn), barra inferior reordenada, `onNavigateCatalog`.  
   - Mantener: `onNavigateCookies` y enlace a cookies si ya existen en el proyecto.

2. **Landing / Home (Next.js:** `app/page.tsx` usa secciones tipo Landing)  
   - Aplicar: nuevo copy de descripción; animaciones con `motion` en hero, colecciones y proceso; uso de `AnimatedSection`, `StaggerContainer`, `staggerItem`.  
   - Requiere: instalar/verificar dependencia **motion** (o `framer-motion`) y añadir componente **AnimatedSection** (por ejemplo en `src/components/shared/AnimatedSection.tsx` o dentro del módulo que corresponda).

3. **AnimatedSection (nuevo)**  
   - Crear en el proyecto (p. ej. `src/components/shared/AnimatedSection.tsx`) a partir de V2, manteniendo imports de `motion/react` y tipos existentes.

---

**¿Aplico estos cambios al proyecto Next.js?** (Sí / No)
